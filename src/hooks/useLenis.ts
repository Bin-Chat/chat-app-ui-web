// Stub hook for Lenis smooth scroll
// Install 'lenis' package to enable smooth scrolling
export const useLenis = () => {
  // Smooth scroll via Lenis (optional enhancement)
  // To activate: npm install lenis, then init here
};
